version: '3'
services:
  web:
    build: .
    ports:
      - "8083:8083"
    environment:
      - WEATHER_API_KEY=${WEATHER_API_KEY}
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev 